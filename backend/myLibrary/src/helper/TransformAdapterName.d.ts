// transformAdapterName.d.ts

/**
 * Transforms a class/adapter name by removing the "Adapter" suffix if present.
 * @param name The original adapter name
 * @returns The transformed name without "Adapter" suffix
 */
export declare function transformAdapterName(name: string): string;
